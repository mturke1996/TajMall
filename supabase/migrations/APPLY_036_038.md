# تطبيق إصلاحات الإيجار والخزينة (036 → 038)

نفّذ في **Supabase → SQL Editor** بالترتيب (ملفاً ملفاً أو دفعة واحدة):

1. `036_rent_links_sync_on_journal_reverse.sql`
2. `037_audit_complete_and_reverse_cashbox_sync.sql`
3. `038_rent_charge_dedupe_repair.sql`
4. `verify_accounting_ready.sql` — يجب أن يظهر: **قاعدة البيانات جاهزة**

من المشروع محلياً:

```bash
npm run audit:rent
```

لتشغيل التطبيق إذا كان المنفذ 3000 مشغولاً:

```bash
npm run dev:fresh
```
